# Decisions

Architectural Decision Records for `product-lifecycle-templates`, following the [MADR v4](https://github.com/adr/madr) standard.

## Format

- One file per decision: `NNNN-short-kebab-title.md`
- Numbered sequentially (0001, 0002, ...) by record-creation order. Records 0001 through 0008 were transcribed in one batch on 2026-07-11 from the strategy brief, the plan, and the shape of the code itself, so their numbers follow decision date rather than creation date; the `date` field is always the date the decision was actually made.
- YAML frontmatter: `status`, `date`, `decision-makers`
- Immutable once accepted. A new decision supersedes an old one rather than editing it.

## Status lifecycle

| Status | Meaning |
|---|---|
| `proposed` | Under consideration, not yet decided |
| `accepted` | Decision made, in effect |
| `deprecated` | No longer recommended, but not replaced |
| `superseded by ADR-NNNN` | Replaced by a newer decision |

## When to create an ADR

Create when:

- The decision shapes the library's structure, its metadata contract, or its authoring rules
- Multiple alternatives were seriously considered
- A future reader (human or agent) would ask "why was this done this way?"
- The decision is hard to reverse, or looks wrong-but-intentional from outside

Do not create for:

- Implementation details (a variable name, a minor refactor)
- Anything already stated in the methodology or the gate

## The records

| # | Decision | Date |
|---|---|---|
| [0001](0001-repo-and-package-name.md) | `product-lifecycle-templates` is the name everywhere; the `pm-` prefix is dropped | 2026-06-29 |
| [0002](0002-variant-model.md) | Ship lean/full variants with descriptive filenames and a strict nesting rule | 2026-06-29 |
| [0003](0003-phase-vocabulary.md) | Use lowercase phase values matching pm-skills frontmatter | 2026-06-29 |
| [0004](0004-first-family-and-bundle.md) | Build `delivery-docs` first, starting with the PRD bundle | 2026-06-29 |
| [0005](0005-bundle-ids-doctype-spine.md) | Bundle IDs are bare document-type handles; phase lives in metadata, never in the path | 2026-06-30 |
| [0006](0006-guidance-style-approach-a.md) | Carry section guidance as enriched HTML comments | 2026-06-30 |
| [0007](0007-research-log-as-bundle-artifact.md) | The research log is a committed bundle artifact | 2026-06-30 |
| [0008](0008-gate-python-local-interim.md) | Ship the governance gate as a local Python script, as a decided interim | 2026-07-03 |
| [0009](0009-scaffold-graduation-flat-templates.md) | Graduate the library out of `_local/` to a flat `templates/` scaffold | 2026-07-12 |
| [0010](0010-meta-declares-size-contract.md) | The meta declares the size contract; single-size bundles are a legal shape | 2026-07-12 |
| [0011](0011-madr-v4-at-docs-internal-decisions.md) | Adopt MADR v4, locate records here, and correct rather than supersede transcription errors | 2026-07-13 |
| [0012](0012-evidence-outranks-catalog-and-paired-skill.md) | Primary evidence outranks the catalog and the paired skill; conflicts are reported, not absorbed | 2026-07-14 |
| [0013](0013-local-split-and-going-public.md) | Split `_local/`: promote what public docs cite into `docs/internal/`, untrack the rest, and go public | 2026-07-14 |
| [0014](0014-gate-may-use-pyyaml-for-frontmatter-validity.md) | The gate may use PyYAML for one check (frontmatter validity); the other six stay pure stdlib | 2026-07-16 |
| [0015](0015-second-taxonomy-axis-phase-xor-classification.md) | A bundle declares a `phase` XOR a `classification`, never both, never neither (closes TX-1, unblocks the metadata schema) | 2026-07-17 |
| [0016](0016-adopt-machine-checkable-metadata-schema.md) | A machine-checkable metadata schema (`tools/meta.schema.json`) is the meta contract, validated in CI (WP-21, from RFC-0001) | 2026-07-17 |
| [0017](0017-gate-may-use-jsonschema-for-meta-validation.md) | The gate may use a JSON Schema validator for one more check (meta validation); the stdlib checks stay pure | 2026-07-17 |
| [0018](0018-machine-catalog-generated-manifest.md) | The machine catalog is a generated `manifest.json`, committed to VC and kept fresh by the gate (WP-22, delivers RFC-0001's second artifact) | 2026-07-17 |
| [0019](0019-selection-metadata-and-approx-tokens.md) | Selection metadata: authored `default_size` + `sizing_guidance`, and a generated heuristic `approx_tokens` (no tokenizer dependency) (WP-23) | 2026-07-18 |
| [0020](0020-adopt-delivery-docs-family-contract.md) | Adopt the delivery-docs family contract, enforced by gate check K; methodology is descriptive, not a membership rule (WP-24) | 2026-07-20 |
| [0021](0021-complete-the-tier-1-floor.md) | Complete the Tier-1 floor on a schedule; grow-by-pull governs Tier-2/Tier-3 only (adopts the buildout plan) | 2026-07-20 |
| [0022](0022-adopt-decision-docs-family-contract.md) | Adopt the decision-docs family contract (rfc, adr, sdd), enforced by gate check K; the second family contract | 2026-07-21 |
| [0023](0023-resolve-the-tier-1-family-taxonomy.md) | Resolve the Tier-1 family taxonomy (D-A, D-B): definition-of-done, strategy-docs, and ops-docs; a family is coherent on one axis, and a contract may gate a set of axis values | 2026-07-22 |
| [0024](0024-adopt-governance-docs-family-contract.md) | Adopt the governance-docs family contract, the first on the classification axis; resolves kpi-dashboard to classification: utility | 2026-07-22 |
| [0025](0025-executable-tests-for-gate-logic.md) | Executable tests complement the structural gate for logic with no live subject; they live in tools/, run in CI, and block merge | 2026-07-23 |
| [0026](0026-adopt-qa-docs-family-contract.md) | Adopt the qa-docs family contract (test-plan, test-case, bug-report) at phase develop; two families may share a phase, and examples may chain across a family boundary | 2026-07-25 |
| [0027](0027-adopt-strategy-docs-family-contract.md) | Adopt the strategy-docs family contract (product-vision, product-strategy, product-roadmap, okrs); the first to gate a set of axis values, and the first live use of a capability built ahead of its subject | 2026-07-25 |
| [0028](0028-adopt-a-format-axis.md) | Adopt a format axis orthogonal to size: a bundle may ship a document in more than one shape, nesting applies within a format and never across formats | 2026-07-25 |
| [0029](0029-gate-the-research-log-contract-not-its-layout.md) | Gate the research log's contract (per-source retrieval record), not its layout; three numbered layouts are legal. Closes DF-2 for 10 of 16 logs; opens DF-4, the six table logs that carry no URL. Carries a dated correction: the premise that three logs were status-less was false | 2026-07-27 |
| [0030](0030-templating-scope-markdown-documents.md) | This library templates Markdown documents; artifacts whose primary form is visual, executable or a live data surface are named out of scope rather than left silently unbuilt. Applies to catalog 52 (wireframe) and 54 (interactive prototype), adds `prototype-brief` as a new type in `discovery-docs`, and reports the floor as 25 of 27 templatable | 2026-07-30 |
| [0031](0031-adopt-discovery-docs-family-contract.md) | Adopt the `discovery-docs` family contract (business-case, user-persona, prototype-brief) on `phase: discover`. The first family to extend the worked thread **backward** in time, defining the Recurring Analyst that sixteen files already reference; and the first ratified with a **provisional member**, `prototype-brief`, whose membership is conditional on its own research passing ADR 0030's admission test | 2026-08-04 |
| [0032](0032-adopt-standing-standards-family-contract.md) | Adopt the `standing-standards` family contract (definition-of-done, runbook) on a **set**: `classification: foundation` or `tool`. The first family to pair those two values, on the cut "is it a standard you are judged against, or an instrument you execute?". Ratified with its own falsifier attached, because its coherence is grouped by cadence rather than by job | 2026-08-05 |
| [0033](0033-adopt-process-docs-family-contract.md) | Adopt the `process-docs` family contract (sprint-retrospective-notes, incident-postmortem) on `phase: iterate`. The family exists so the retro-versus-postmortem distinction is taught by contrast; its examples may not blur, and a postmortem example must leave at least one action open, owned and dated | 2026-08-05 |
| [0034](0034-adopt-communication-docs-family-contract.md) | Adopt the `communication-docs` family contract (status-report) on `classification: utility`. A one-member family, stated rather than hidden, whose members **own none of their own facts**: every figure in an example must be read from an artifact already in the library, and disagreeing with it is a contract failure | 2026-08-05 |
| [0035](0035-prototype-brief-fails-the-admission-test.md) | **`prototype-brief` fails ADR 0030's admission test and does not ship**, closing `discovery-docs` at two members. Six research dimensions and 29 sources found prototyping practice everywhere and a commissioning **document** nowhere: GOV.UK ships a code toolkit, GV's Sprint Brief scopes a whole sprint, Strategyzer's is a card, and every assumption-test ancestor stops at a canvas. The second time the test has changed an outcome rather than ratified one, and the first time it has removed a type already written into a contract and a backlog | 2026-08-05 |
| [0036](0036-library-prefix-and-skill-under-skills.md) | **Adopt the `plt-` component prefix and move the skill from the repository root to `skills/plt-fill-template/`**, reaching **convergent** tier on the Advanced Skill Library Standard. Not a compliance chore: the root `SKILL.md` was almost certainly never discoverable, because the Agent Skills spec requires a skill's `name` to match its parent directory and the Claude Code plugin loader scans `skills/`. The plugin name is unchanged; only the component inside it is prefixed. The installed skill's name does change. **Corrected 2026-08-08:** the root `SKILL.md` WAS discoverable by the skills CLI, which looks for one by design; only the spec/plugin-loader half of that claim stands, and the correction matters because removing the root file switched the CLI's subdirectory search on (see 0037) | 2026-08-08 |
| [0037](0037-keep-the-build-harness-off-the-published-skill-surface.md) | **Move the maintainer-internal build harness off the published skill surface, and gate the surface rather than trusting the layout.** The first ever run of `npx skills add` installed **two** skills; the second was the build harness, whose own description says it is not for library users. Removing the root `SKILL.md` in 0036 removed a short-circuit that had been suppressing the CLI's subdirectory search. The harness becomes a slash command at `.claude/commands/`, invisible to both the CLI and the Standard, verified against the real repository. `tools/check-export-surface.py` is the durable half: it asserts the exported skill set equals what `library.json` declares, in both directions, across the CLI's 31 scanned prefixes including `.codex/skills` | 2026-08-08 |
| [0038](0038-what-the-circularity-signature-obliges.md) | **Accepted 2026-08-14.** What the measured **circularity signature** obliges: two independent runs put the templates at **+0.85** on criteria drawn from their own guide beside **-0.03** on criteria drawn from neither. Proposes a decision procedure, not a template edit, and states the trap plainly: adding the held-out criteria makes them no longer held out, so **"the eval said so" is not an admissible reason** and any later run needs fresh criteria. **Substantially revised 2026-08-10**, withdrawing its own first recommendation: proposes an **element admission test** (E1 sourced, E2 or labelled, E3 homed, E4 sized) plus a **three-track triage** routing any candidate to a rubric row, a house convention, or a template element, so the decision becomes a rule application rather than a judgment call. Two of the four properties leave the scope conversation at step 1. Records two missing rules as the reason the question was undecidable: ADR 0030's admission test **governs document types and has no element-level form**, and the protocol sets **no target for the held-out gap at all**, so `-0.03` is neither a pass nor a fail | 2026-08-08 |
| [0039](0039-maintainer-discretion-replaces-the-pull-gate.md) | **The maintainer may build any template; grow-by-pull becomes an input, not a gate.** Amends [0021](0021-complete-the-tier-1-floor.md) for Tier 2 and Tier 3. With the floor complete, zero external users and **zero issues ever filed**, a strict pull gate is not a gate but a stop: 151 Tier-2 and 27 Tier-3 candidates could never be built, because no population exists from which a pull could arrive. The pull queue is retained as an intake and as a **priority** signal. **What does not change is the load-bearing half**: coverage and real usage stay separate honest numbers, "zero real fills" stays visible, and every bundle still faces the full pipeline and 0030's admission test. Discretion is about *which* types get built, never about the standard. Carries its own falsifier: re-open at roughly forty bundles with still-zero external fills | 2026-08-14 |
| [0040](0040-free-and-open-source-no-paid-tier.md) | **Free and open source under Apache-2.0, with no paid tier**, funded by the maintainer's time and functioning as an authority funnel rather than a product that charges. Closes **VL-1**, open since 2026-07-02 with no stated cost and no scheduled date, which blocked the site track from choosing a domain or a call to action. Open core was rejected for now because it needs entitlement machinery before it has content, and the tier that would most plausibly carry a price is separately closed as "no" (D4). Names its own accepted cost: the maintenance is funded by time, indefinitely, with no mechanism that scales it, which is the **counterweight to [0039](0039-maintainer-discretion-replaces-the-pull-gate.md)** removing the rule that limited how many bundles may be built | 2026-08-14 |
| [0041](0041-maintainer-preference-sets-the-build-order.md) | **Maintainer preference and need set the build order; external demand is an input, not a rank.** Amends [0039](0039-maintainer-discretion-replaces-the-pull-gate.md), which removed the pull gate on *whether* a type may be built and kept the queue as a *priority* signal. **That signal has been empty since the intake shipped 2026-08-07 and there is no population from which it could arrive**, so rule 1 could never fire and rule 2 valued the only real input in the system at one unit of a currency whose total supply is one unit. Ranking by an always-zero input is not ranking; it presents the maintainer's own judgment as though a queue produced it. The intake keeps its two useful jobs, recording a request without committing to it and making a refusal legible, and loses the one it never performed. **`pull-gated` becomes `candidate`**, since the name asserts a gate this record removes. **Tier 3 is not reopened**, and the record names that D4's stated reopening condition, a pull from a real regulated team, is now written in a currency this record no longer ranks in. **Names its own cost plainly:** it removes the last structural brake against building content nobody uses, which the roadmap's risk table warns against by name, and it tightens 0039's falsifier rather than inheriting it quietly | 2026-08-22 |
| [0042](0042-epic-joins-delivery-docs.md) | **`epic` joins the delivery-docs family, and the first Tier-2 build finds a gate two records did not remove.** [0039](0039-maintainer-discretion-replaces-the-pull-gate.md) removed the gate on *whether* a Tier-2 type may be built and [0041](0041-maintainer-preference-sets-the-build-order.md) removed the rule that ordered it; **neither touched family membership, which is enumerated and which the contract says may only change by decision record**, so the first build attempt stopped before phase 0. Amends [`contracts/delivery-docs.md`](../contracts/delivery-docs.md) to 0.1.3, placing `epic` in the section 1 chain sentence between the artifact that opens the chain and the stories that decompose it. The chain claim is tested against research already fetched and verified in this repository rather than newly asserted, per procedure 11. Uses the `queued` block in [`state-overrides.json`](../../../atlas/state-overrides.json), empty since it shipped, rather than writing a second backlog document. **Names the cost it does not remove:** eight other families will hit the same gate on their first Tier-2 admission, and generalising the rule on one instance is refused because that is how the demand rule got written | 2026-09-01 |
| [0043](0043-the-usage-gate-becomes-advisory.md) | **The usage gate becomes advisory, and the honesty gate does not move.** No usage signal gates any build: [0041](0041-maintainer-preference-sets-the-build-order.md)'s forty-bundle falsifier becomes a soft reminder rather than a trigger, the roadmap's risk-table entry stops reading as a prohibition, and **M5 is no longer gated on an M3 usage signal**. Building was already ungated by [0039](0039-maintainer-discretion-replaces-the-pull-gate.md) and 0041 and did not feel ungated, because each left a falsifier that re-imposed the gate at a threshold. **Half the decision is what does not move:** a bundle stays `beta` until a real usage cycle is recorded, is never called proven on a green gate, and zero real fills is still published as zero. **A library may grow without evidence; it may not claim without evidence.** Adds [`evaluating-a-template.md`](../../how-to/evaluating-a-template.md), because the gate demanded feedback and never supplied a method. Names its own cost: this removes the last structural pressure toward outreach | 2026-09-03 |
| [0044](0044-the-section-schema-is-a-second-generated-artifact.md) | **The section schema is a second generated artifact beside the manifest, and its parser is the guidance grammar's first mechanical reader.** `sections.json` is generated from the template variants themselves and `--check`ed in CI, following [0018](0018-machine-catalog-generated-manifest.md). It ships **beside** the manifest, not inside it: embedding measured **4.6x** on the artifact agents read to *select*, while the AG-2 spec caps default responses at 1,200 tokens. Three departures from the 22-line AG-1 sketch, each measured: per-format grouping with `in_sizes` rather than `in_lean`/`in_full`, because two titles appear in more than one format with different guidance; `has_table` **and** `has_row_hint`, because the two signals disagree on **10 of 353** sections; and beside rather than inside. **The part with teeth:** the generator fails on a comment it cannot parse, so the Approach A grammar is enforced rather than conventional for the first time, and a malformed comment now fails a build that would previously have passed. Corrects the LP-1 spec's own extraction regex, which extracts zero of 353 WHAT fields | 2026-09-05 |
| [0045](0045-the-mcp-server-is-python-and-lives-in-this-repository.md) | **The MCP server is Python and lives in this repository, not a TypeScript package mirroring `pm-skills-mcp`.** The 2026-07-12 sketch specified a separate npm package with an embed step packaging `templates/**` into the artifact. Rejected on three facts: the recommended install route already **clones this repository**, so there is nothing to embed; two of the five tools are already Python here, so wrapping is an `import` where mirroring is a rewrite plus a permanent parity obligation; and an embedded copy would be a **fifth artifact that can drift**, which is the defect class the whole gate exists to catch. **The part with teeth:** the server CALLS `validate-fill.py` and `strip-template.py`, so agreement with the CLI is true by construction. Names its cost plainly - two sibling repositories now serve MCP differently. Building it falsified three more spec claims, including one of our own: the taxonomy axis is `phase` XOR `classification`, so the sketch's phase-only filter left **10 of 27 bundles unreachable** while returning plausible results | 2026-09-06 |
| [0046](0046-the-site-is-astro-starlight-under-pattern-s.md) | **The site is Astro plus Starlight under Pattern S, and it brings a Node toolchain this repository does not have.** Adopts [`site-plan.md`](../site-plan.md) and reverses three calls in the untracked 2026-07-17 proposal, which **cites the family site standard zero times** and conflicts with it on the framework (14.2), the content mount (14.1) and the deploy shape (14.6). The mount is not a style question: the standard records a **build test** showing an app in `site/` with content at repo root fails to build, because bare Starlight component imports resolve only inside the app. **The part with teeth:** this Python-only repository takes on **npm, a lockfile, `npm ci` and a permanent second dependency-update surface** - the real decision hiding inside "build a site", which no version of the plan had named. Effort was under-described by roughly an order of magnitude: "1 to 2 days" is withdrawn, not adjusted, against a measured donor of **1,496 lines**. The conformance checklist is **explicitly provisional**, because Section 14 has not landed: the pinned Standard `v1.10.0` carries sections 1 through 12 and no more | 2026-09-11 |
| [0047](0047-the-usage-precondition-leaves-the-language-too.md) | **The usage precondition leaves the language too, and the honesty gate stays.** [ADR 0043](0043-the-usage-gate-becomes-advisory.md) removed the usage permission gate on 2026-09-03 and **the prose did not follow**, so `roadmap.md` and `STATE.md` went on calling M3 "the binding constraint" - and on 2026-09-11 a **new** sequencing rule gating the site on WP-31 was written into `site-plan.md` by an agent that had read ADR 0043, hedged as "a preference, not a prohibition", the exact hedge 0043 said had been read as a ban the first time. **A rule that regrows in a new document was never repealed, only moved.** So the language goes. **What does not move, for the second time on purpose:** the honesty gate. All 27 bundles stay `beta`, nothing is called proven on a green gate, and zero real fills is still published as zero - removing a constraint on what may be BUILT and one on what may be CLAIMED are different acts. Also records two maintainer evaluations: `docs/releases/v0.6.0.md` does **not** count as the first real usage cycle (it is stamped `filled_by: agent:claude-opus-5` - every participant in that loop is inside the house), and distribution submissions wait on judgment rather than on any gate | 2026-09-11 |
| [0048](0048-one-named-source-clears-the-admission-test.md) | **One named source clears ADR 0030's admission test, and `spike-report` is admitted on that reading.** The first candidate to land in the gap between a clean pass and a clean failure: **exactly one** qualifying source (Microsoft's Code with Engineering Playbook, which says the deliverable "should be a document" and ships a template, read from the raw markdown) against **the term's own inventors**, whose c2 account describes the output as throwaway **code**. The real question was never about spikes: does ADR 0030 require one source or a preponderance? **The text says "a named source", singular**, and answering that silently by building the bundle would have set the precedent without anyone noticing. **It does not reopen `prototype-brief`**, which failed with **zero** - one and zero are different numbers and that is the whole distinction; an admission argued on preponderance-of-practice WOULD have reopened it. Raising the bar to two sources is a legitimate alternative, rejected on timing: changing a rule inside the record that applies it makes the change indistinguishable from the outcome. **The obligation:** the bundle must teach the dispute, not resolve it | 2026-09-12 |
| [0049](0049-pi-release-retrospective-fails-the-admission-test.md) | **`pi-release-retrospective` fails ADR 0030's admission test and does not ship; `process-docs` grows to three.** Scaled Agile's own Inspect and Adapt Facilitator's Guide, **read in full and freely available**, names the event's outputs as a predictability score, tacit understanding, and improvement **backlog items** - no document anywhere. The two vendor candidates are a **4-column board** (which ADR 0030 already refused for `wireframe`) and a template scoped to a single-team release rather than the multi-team event. That is the `prototype-brief` shape: every candidate is a neighbouring artifact under another name. **The structural finding is worth more than the refusal:** *cadence retrospectives feed a backlog; terminal retrospectives produce a document* - the Scrum Guide describes the Sprint Retrospective's output exactly as SAFe describes I&A's, so this is the shape of the whole cadence line. **One research pass, two opposite verdicts**, because `project-milestone-retrospective` was admitted twice over in the same pass. Does NOT reopen `sprint-retrospective-notes`, which was admitted on document-shaped vendor templates that have no Type-B equivalent | 2026-09-13 |
| [0050](0050-qa-docs-admits-a-fourth-member.md) | **`qa-docs` admits a fourth member, and the exclusion clause is what governs a candidate.** The contract answers membership **twice, differently**: its role inventory ends "or reports a verification that **failed**" (which is the bug report), while its exclusion clause - the sentence written to test a candidate - reads "not to plan, specify, or **report the verification of a product increment**", with no failure qualifier. A test summary report matches the second exactly. ADR 0026 has **no fourth-member clause**, unlike ADR 0022, so nothing had settled it and building on it silently would have set the precedent unnoticed. **The generalisable rule:** where a family contract's exclusion clause and its role inventory disagree, **the exclusion clause governs**. Amending the contract instead was rejected on ADR 0048's ground - changing a rule inside the record that applies it makes the change indistinguishable from the outcome. **Removes the family objection only:** ADR 0030's scope test is separate, answered by research, and open when this was written - if it fails, the type still does not ship, as `pi-release-retrospective` did not | 2026-09-14 |

| [0051](0051-site-dependency-updates-follow-the-family-dependabot-pattern.md) | **Site dependency updates follow the family Dependabot pattern, and no `npm audit` gate is added.** The one part of taking on Node that [ADR 0046](0046-the-site-is-astro-starlight-under-pattern-s.md) did **not** settle: `site-plan.md` section 9's sixth item, the dependency-update policy, which no record decided and for which this repository has **no `.github/dependabot.yml` at all** - not even for the GitHub Actions it already uses. Adopts `pm-skills`' configuration for `npm` at `/site`: **weekly, grouped, `minor` and `patch` only**, majors as individual PRs. **No `npm audit` gate**, refused on actionability rather than safety - a static-site tree is mostly build-time, and an advisory a maintainer can only wait out should not block a docs deploy; the predictable end state of such a gate is `|| true`, which is DF-7's shape from the other direction. **Deliberately not claimed:** that the site has no client-side surface, because Starlight ships browser JavaScript. **The cost, named:** a weekly PR stream in a repo that has never had one, and a policy that buys notification rather than maintenance. Decided under explicit maintainer delegation. **Names and does not decide** the GitHub Actions ecosystem gap | 2026-09-15 |
| [0052](0052-the-site-is-a-standalone-property-at-the-github-io-path.md) | **The site is a standalone property at `product-on-purpose.github.io/product-lifecycle-templates`, not an umbrella section or a custom domain.** Closes [`site-plan.md`](../site-plan.md) section 14 item 4, which turned out to be **one question in the plan and two in practice**: branding could wait, the URL could not, because clause 14.7 requires `site` and `base` to be set once before anything is built. Standalone is decision **A-4 of the family standard applied, not a new choice** - A-4 already picked per-plugin sites over a portal, and there is no `product-on-purpose.github.io` umbrella repository to be a section of. **The family is genuinely split on the URL half**: `pm-skills` and `agent-skills-toolkit` serve from `github.io`, `thinking-framework-skills` from a `productonpurpose.com` subdomain, so this picks the less branded side and says so. **Acceptable because it is one of the few reversible decisions in the site track** - GitHub Pages redirects the `github.io` URL once a custom domain is configured, so links published meanwhile keep working. A custom domain now would be presentation ahead of substance for a library with **zero external users** | 2026-09-20 |
| [0053](0053-launch-coordination-checklist-joins-standing-standards-as-a-tool.md) | **`launch-coordination-checklist` joins `standing-standards` as a `tool`, not `delivery-docs`.** Unblocks a spec stalled since 2026-09-11, where the catalog category `Release / Deployment / Runbooks` names both families' territory in one string. **Settled by the family's own falsifier rather than by its membership test**: [ADR 0032](0032-adopt-standing-standards-family-contract.md) conceded that this family groups by rhythm rather than by job, and set the condition under which it would be wrong - a candidate matching the cadence but **not consulted at the moment of action**. A launch checklist is consulted at the moment of action, so it is the opposite of the candidate that would force a split. `tool` rather than `foundation` on the contract's own cut, an instrument you execute rather than a standard you are judged against, making it a sibling of `runbook` with which it already shares SRE lineage. **Not `delivery-docs`** because that contract obliges every member to state its position in a chain carrying one unit of product work, and this carries none. **The counter-argument is recorded**: pm-skills files its paired skill under the `deliver` phase. **A second find, more urgent than the assignment**: the promise tag read `future:launch-checklist` while the catalog id is `launch-coordination-checklist`, and `check-bundles.py` resolves `future:` by bundle id, so building under the catalog id would have left an unfulfillable promise **with the gate still green**. Tag corrected | 2026-09-20 |
| [0054](0054-the-mcp-server-returns-a-uniform-envelope.md) | **The MCP server returns a uniform `{ok, data?, error?}` envelope, and `ok` stops meaning three things.** Closes [`ag2-mcp-spec.md`](../ag2-mcp-spec.md) section 5's open decision, the one blocking `structuredContent`. **The annotation was never the fix**: adding one was measured to *cause* a regression, because the SDK validates returns against the schema and every tool's error shape differs structurally from its success shape, so annotating raises `ToolError` on every refusal. The contract has to be uniform first. **The find this ADR adds**: `ok` is already overloaded three ways in the shipped server, meaning the document is valid, the CLI exited 0, and your request was bad, so the naive envelope renders an invalid document as `{ok: true, data: {ok: false}}`. Inner fields renamed `data.valid` and `data.refused`. **Measured, not recalled**: three annotation shapes spiked against the installed SDK on both the success and the refusal path; per-tool `TypedDict` wins because the pydantic union nests under `result` and a flat `TypedDict` types `data` as `dict[str, Any]`, asserting nothing. Breaking change to a shipping wire format, taken now because **nothing external calls the server**. **Out of scope, deliberately**: the `mcp<2` port, and whether v2 handles annotations identically is **not verified** | 2026-09-20 |
| [0055](0055-retire-the-zero-fills-disclosure.md) | **Retire the zero-fills disclosure, and keep the ban on claiming a bundle is proven.** This library stops publishing a real-fill count on any public surface and stops requiring one: the landing-page card, the `<Aside>` on all 30 generated bundle pages, the index line, the prose in `README.md` / `AGENTS.md` / `STATE.md`, site-plan clause 13.4, site-s0-spec AC-17's third clause, and the `status` row of all nine family contracts, which gated `stable` on "one real usage cycle is recorded". **What is NOT retired is the load-bearing half**: no page may call a bundle **proven**, **verified** or **validated**, still enforced by `gen-site.mjs --check` between `astro build` and `upload-pages-artifact`, still mutation-tested, and every bundle stays `beta`. **The distinction the decision rests on: declining to volunteer a negative number is a different act from asserting a positive one**, and only the second would be a claim this library cannot support. **No bundle is promoted** - removing a graduation rule is not exercising it. Graduation becomes the maintainer's judgement that a bundle has settled, replacing a rule that waited on an event outside the project's control and so was never a path at all. **The cost, named:** a reader can no longer tell from the site how much real-world use these templates have had. ADRs 0039, 0043 and 0047 each affirmed the disclosure and are **superseded on this point and left unedited** | 2026-09-21 |
| [0056](0056-build-cost-is-measured-and-ingestion-is-separate-from-verification.md) | **Build cost is measured from harness transcripts, and ingestion is deliberately separate from verification.** Two documents stated a per-bundle build cost, disagreed with each other ("0.6-1M tokens" against "700K-1M"), and neither cited a measurement; measured, both were low by **20 to 30 times** at 21M to 25M weighted token-equivalents. The data had been written on every subagent turn since the beginning and nothing had ever read it. Reports live in a new top-level `bundle-builds/`, keyed by bundle and `template_version` so a rebuild lands beside the record of what the last one cost. **The structural decision, and the one that looks wrong from outside**: `--ingest` and `--check` are separate commands and **the CI gate cannot verify the reports it gates**, because transcripts are machine-local, absent on the runner and pruned eventually; a measurement whose source can disappear is captured when it is true, and the committed JSON is the source of truth thereafter. A report is therefore trusted because it was generated once, not because it can be re-derived, which is weaker than every other generated artifact here and is stated rather than implied. **The runbook's claim about where cost goes was also wrong**: drafting is the largest stage, not the research fan-out and review. Rejected: a ninth bundle file (would amend nine family contracts), a section in hand-authored `_history.md`, and fields on `manifest.json` (the same reasoning that moved section detail to `sections.json`). **Does not rank quality**, divide the orchestrator's spend per bundle, or record reasoning effort. **Corrected 2026-09-22:** the generator counted each API response two to three times, because the harness writes one transcript record per content block and each repeats the whole usage block; corrected, a build is about **10M to 12M** weighted, or **$33 to $41** at API list rates, the estimates were low by roughly 10 to 20 times, and the review is not under half of research. The decision is unchanged; the Correction section prices the weakness it accepted. **Its open question was resolved the same day**: the builds that drafted on Opus never ran `build-bundle.js`, whose first real build drafted on Sonnet as pinned | 2026-09-22 |
| [0057](0057-issue-log-joins-governance-docs-as-a-fourth-member.md) | **`issue-log` joins `governance-docs` as a fourth member, `classification: utility`.** The membership test already names the type ("a continuously-maintained register, log, or dashboard ... to track risk, open items, or performance"), but [ADR 0024](0024-adopt-governance-docs-family-contract.md) was written for three members with **no fourth-member clause**, the same gap [ADR 0050](0050-qa-docs-admits-a-fourth-member.md) settled for `qa-docs`. **An issue log stands to the RAID log's I exactly as the risk register stands to its R**: the deepened, standalone form of one quadrant, which `raid-log_guide.md` already describes. Not `qa-docs` (it verifies nothing) and not `standing-standards` (that contract routes anything "revised on a cadence and valuable only while current" here). Contract to `0.2.0`; no obligation changes. **Does not decide admission**, which rests on retrieval in `tier2-specs.md` | 2026-09-23 |
| [0058](0058-definition-of-ready-joins-standing-standards-as-a-foundation.md) | **`definition-of-ready` joins `standing-standards` as a `foundation`, not a `tool`.** The contract forecast the type in 2026-08 but never said which value it would take. **Settled by the contract's cut applied to mechanics, not cadence**: a standard items are judged against, agreed and applied by the same team in a scheduled conversation, the entry-side mirror of `definition-of-done`. Being consulted at the moment of action is the family's membership falsifier, not its classification test, which is why the launch checklist's `tool` does not transfer. **Its failure mode inverts the one the contract names for `foundation`**: not agreed and never honoured, but honoured too hard, as a gate, so its review trigger fires in both directions. Contract to `0.3.0`. **Does not endorse keeping a DoR**: the bundle carries the dispute its siblings already report | 2026-09-23 |
| [0059](0059-announcement-internal-comms-joins-delivery-docs.md) | **`announcement-internal-comms` joins `delivery-docs` at `phase: deliver`, not the `communication-docs` family that forecast it.** That contract named "a release announcement distinct from `release-notes`" as a likely member, and its membership test fits; **its only axis value does not**: `classification: utility` was justified as "maintained, periodic", and an announcement is written once and never revised. `delivery-docs` already admits a type that "announces a unit of product work", so the chain sentence gains the internal announcement beside the release note: one tells the people who use the product, the other tells the rest of the organization, **a boundary this library draws itself**. The forecast is corrected in place with a date; `communication-docs` stays at one member. Contract to `0.2.0` with 0060. **Does not decide admission** (three vendor and practitioner sources, in `tier2-specs.md`) | 2026-09-25 |
| [0060](0060-change-request-joins-delivery-docs.md) | **`change-request` joins `delivery-docs`, and the family's membership test gains the verb "changes".** **The first admission that widens a membership test**: tested against all nine contracts, a per-occasion change request fitted none, and `governance-docs` names "an event-driven or phase-bound artifact" as out. Serves the **project and product baseline** sense (PMI: "A formal proposal to modify a document, deliverable, or baseline"), which the library's own routing already used; IT service change to a running system, and DORA's finding against external change-board approval, stay a named neighbour, and the standing change log stays a separate `governance-docs` candidate. Rejected: a new one-member family, widening `governance-docs`, and building the log instead. Contract to `0.2.0` with 0059 | 2026-09-25 |

## Correction versus supersession

The rule, from [0011](0011-madr-v4-at-docs-internal-decisions.md):

> A **factual error** in a record gets corrected in place, with the correction dated and the error named. A **change in the decision itself** gets a new number.

[0003 (phase vocabulary)](0003-phase-vocabulary.md) is the worked example. It was transcribed with a false claim about the pm-skills phase enum, but the decision it records (lowercase values matching pm-skills) never changed. Superseding it would tell a future reader that this project once believed something different about phase casing, which it never did. So it carries a dated Correction instead, and the version history shows exactly what was believed and when it was fixed. A record that silently rewrites itself is worth less than one that shows its scars.
