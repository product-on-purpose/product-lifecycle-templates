# Pinned pm-skills skill IDs that a bundle's `pairs_with` may reference.
#
# WHY PINNED, AND NOT DISCOVERED.
# pm-skills is a separate repository. This gate must run in CI, on a clean checkout, with no
# network and no sibling clone on disk, so it cannot resolve a skill ID by looking. A pinned
# list is the honest alternative: it is a deliberate assertion about a neighbour, updated by a
# human who checked, rather than a guess.
#
# THE FAILURE THIS PREVENTS.
# `pairs_with` claims "an agent invoking this skill should reach for this template." A typo, or
# a skill that never shipped, makes that claim silently false. Nothing else in the repo checks
# it. Cross-repo resolution proper is out of scope (roadmap WP-11 notes it); this catches the
# cheap half: a value that is not a known skill at all.
#
# HOW TO UPDATE.
# Verify against the pm-skills repo before adding a line:
#     git -C ../pm-skills ls-files --error-unmatch skills/<id>/SKILL.md
# Note that `.claude/skills/` is gitignored in pm-skills; `skills/` is the tracked source.
# Add the ID, and say in the commit message that you verified it and when.
#
# Verified present in product-on-purpose/pm-skills @ skills/<id>/SKILL.md on 2026-07-17.
# A bundle with no paired skill declares `pairs_with: []`, which is legal and is not a gap to
# paper over: the `rfc` bundle does exactly that, because no `develop-rfc` skill exists
# (checked 2026-07-17; recorded as finding EC-3 in STATE.md).

deliver-prd
deliver-user-stories
deliver-acceptance-criteria
deliver-release-notes
develop-adr

# Added 2026-07-25 for the qa-docs family. Verified present at skills/deliver-edge-cases/SKILL.md in
# product-on-purpose/pm-skills (tracked source, not the gitignored .claude/skills/ build copy). It is
# the only pm-skills skill any qa-docs member can honestly pair with: its own description names
# "during QA planning to identify boundary and limit scenarios to test" and "when preparing QA test
# plans". Enumerating all 68 tracked skills the same day found no testing or QA skill at all, which is
# finding EC-4 in STATE.md.
deliver-edge-cases

# Added 2026-07-25 for the strategy-docs family. All three verified present at skills/<id>/SKILL.md in
# product-on-purpose/pm-skills (tracked source, not the gitignored .claude/skills/ build copy).
# foundation-okr-writer drafts and reviews OKR sets; measure-okr-grader scores them at cycle close; both
# address the `okrs` bundle directly. foundation-lean-canvas describes itself as serving "framing a new
# product thesis, stress-testing an existing strategy", which is the `product-strategy` bundle's job.
# Worth noting against finding EC-4: pm-skills is strong at the strategy end and absent at the
# verification end, so the gap found by qa-docs is a coverage SHAPE, not a uniform absence.
foundation-okr-writer
measure-okr-grader
foundation-lean-canvas

# Added 2026-09-11 for the `spike-report` bundle specified in docs/internal/tier2-specs.md, which is not
# built. Verified present at skills/develop-spike-summary/SKILL.md in product-on-purpose/pm-skills on
# 2026-09-11 (tracked source, not the gitignored .claude/skills/ build copy): v2.2.0, phase develop,
# 4,027 bytes. Its description draws the boundary this bundle needs - "for the architecture decision the
# spike informs, use develop-adr instead" - so the pairing is the skill's own claim, not ours.
#
# WORTH RECORDING BECAUSE IT WAS NEARLY GOT WRONG IN BOTH DIRECTIONS. A previous session asserted this
# skill existed without checking. Checking it the first time here used a recursive git-tree query that
# came back EMPTY, which looked like a clean refutation and was a truncated response; the direct
# contents call found it immediately. A negative result from one query shape is not a fact. The command
# in this file's header is the one to trust.
develop-spike-summary

# Added 2026-09-13 for the `project-milestone-retrospective` bundle. Verified present at
# skills/iterate-retrospective/SKILL.md in product-on-purpose/pm-skills on 2026-09-13 (tracked source,
# not the gitignored .claude/skills/ build copy): v2.2.0, phase iterate, 5,194 bytes. Its description
# names the occasion this bundle serves in its own words - "use at the end of a sprint, project, or
# milestone".
#
# THAT DESCRIPTION ALSO NAMES THE SPRINT, and `sprint-retrospective-notes` had shipped with
# `pairs_with: []` since 2026-08-07. Nothing recorded whether that was deliberate; the skill plainly
# covers it, so the sprint bundle is paired in the same change rather than leaving one family member
# claiming a pairing its sibling silently declines. `incident-postmortem` stays unpaired: this skill is
# about cadence retrospectives and says nothing about event-triggered failure learning.
iterate-retrospective

# Added 2026-09-22 with the `launch-coordination-checklist` spec (docs/internal/tier2-specs.md), before
# the bundle is built, as the spike-report spec did. Verified present at
# skills/deliver-launch-checklist/SKILL.md in product-on-purpose/pm-skills on 2026-09-22 with the command
# in this file's header, at pm-skills 1cef1a9e (= origin/main), v2.2.0, `phase: deliver`, 3,877 bytes.
#
# LISTING IT HERE IS NOT A PAIRING. This file says the id exists; whether the bundle pairs with it is the
# spec's open question. The skill writes a checklist PER LAUNCH ("1-2 weeks before any significant
# launch"), while the family admits the type as a STANDING instrument, so the pairing holds only if the
# template is what each per-launch copy is made from. If the build finds otherwise, the bundle declares
# `pairs_with: []` and this line stays, because the skill still exists.
deliver-launch-checklist
